
### 💾 **Q70 — NLB Health Checks**

**Application Load Balancer (ALB)**
    
    💡 Layer 7 load balancer with HTTP/HTTPS health checks.
    ➡️ Detects application-level errors and replaces unhealthy EC2 targets.

**Network Load Balancer (NLB)**
    
    💡 Layer 4 load balancer for TCP/UDP traffic.
    ➡️ Does not natively detect HTTP errors without switching to ALB.

**Auto Scaling Health Check**
    
    💡 Replaces unhealthy EC2 instances automatically.
    ➡️ Integrates with ALB for app-level availability.

---

### 💾 **Q71 — DynamoDB Backup & Recovery**

**DynamoDB Point-in-Time Recovery (PITR)**
    
    💡 Continuous backup with per-second restore within 35 days.
    ➡️ Meets RPO/RTO requirements automatically, no manual export.

**RPO / RTO**
    
    💡 RPO = acceptable data loss window; RTO = acceptable downtime.
    ➡️ Use PITR for 15-min RPO, <1-hour RTO recovery.

---

### 💾 **Q72 — S3 Data Transfer Cost**

**VPC Gateway Endpoint for S3**
    
    💡 Private connection to S3 from VPC without using internet.
    ➡️ Avoids NAT Gateway and data transfer fees inside same Region.

**NAT Gateway**
    
    💡 Provides internet access for private subnets.
    ➡️ Incurs extra data processing and egress costs.

---

### 💾 **Q73 — Bastion Host & Private Access**

**Bastion Host (Jump Box)**
    
    💡 EC2 in public subnet used to SSH into private EC2s.
    ➡️ Allow inbound SSH only from trusted on-prem IPs.

**Security Groups**
    
    💡 Stateful firewall for EC2.
    ➡️ Web/app servers allow SSH only from bastion’s private IP.

```
                        ┌───────────────────────────────┐
                        │   On-Premises Network         │
                        │                               │
                        │  Admin Workstation (SSH)      │
                        │  IP: 203.0.113.0/24           │
                        └──────────────┬────────────────┘
                                       │
                         (Internet — SSH connection)
                                       │
              ┌────────────────────────┴────────────────────────┐
              │                 AWS VPC                        │
              │  CIDR: 10.0.0.0/16                              │
              │                                                 │
   ┌──────────┴──────────┐                          ┌───────────┴───────────┐
   │     Public Subnet    │                          │     Private Subnet    │
   │   CIDR: 10.0.1.0/24  │                          │   CIDR: 10.0.2.0/24   │
   │                      │                          │                       │
   │  Bastion Host (EC2)  │                          │  App Servers (EC2s)   │
   │  Private IP: 10.0.1.10│                         │  Private IPs:         │
   │  Public IP: 54.x.x.x │                          │    - 10.0.2.10        │
   │                      │                          │    - 10.0.2.11        │
   │  SG: BastionSG       │                          │  SG: AppServerSG      │
   │  ─ Allows SSH (22)   │                          │  ─ Allows SSH (22)    │
   │    from 203.0.113.0/24 │                        │    from 10.0.1.10     │
   └──────────┬──────────┘                          └───────────┬───────────┘
              │                                                │
              └─────────────── Internal SSH (Private) ──────────┘


```
---

### 💾 **Q74 — Two-Tier Web App Security**

**Web Tier (Public Subnet)**
    
    💡 Hosts web servers accessible over HTTPS (port 443).
    ➡️ Inbound from 0.0.0.0/0, outbound to DB tier only.

**Database Tier (Private Subnet)**
    
    💡 SQL Server backend protected from public access.
    ➡️ Inbound only from web tier’s security group on port 1433.

---

### 💾 **Q75 — Modernizing Multi-Tier App**

**Amazon SQS (Simple Queue Service)**
    
    💡 Decouples microservices with message buffering.
    ➡️ Prevents dropped transactions during high load.

**Amazon API Gateway + AWS Lambda**
    
    💡 Serverless API + compute layer.
    ➡️ Modern, scalable, and minimal operational management.

---

### 💾 **Q76 — On-Prem to AWS Data Transfer**

**AWS DataSync over Direct Connect**
    
    💡 Secure, automated large-scale data transfer.
    ➡️ Reliable, consistent throughput for 10 TB/day workloads.

**Direct Connect (DX)**
    
    💡 Dedicated network link to AWS.
    ➡️ Lower latency and private data path vs. public internet.

---

### 💾 **Q77 — Real-Time Data Ingestion**

**Amazon API Gateway + Kinesis Data Streams**
    
    💡 Fully managed streaming ingestion with API front.
    ➡️ Minimal ops overhead, integrates with Firehose & Lambda.

**Kinesis Data Firehose**
    
    💡 Automatic data delivery to S3 with optional transformation.
    ➡️ Scales automatically, no server management.

---

### 💾 **Q78 — DynamoDB Long-Term Retention**

**AWS Backup for DynamoDB**
    
    💡 Centralized, automated backups with retention policies.
    ➡️ Best operational efficiency for 7-year data compliance.

**S3 Lifecycle Policies**
    
    💡 Automate archival or deletion of old backups.
    ➡️ Reduce cost by moving to Glacier tiers.

---

### 💾 **Q79 — DynamoDB Cost Optimization**

**On-Demand Capacity Mode**
    
    💡 Pay per request, scales instantly for unpredictable traffic.
    ➡️ Ideal for spiky workloads with idle periods.

**Provisioned with Auto Scaling**
    
    💡 Adjusts throughput gradually based on metrics.
    ➡️ Suitable when traffic is predictable.

